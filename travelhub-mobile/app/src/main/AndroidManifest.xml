<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 13+ (API 33+) requiere permiso explícito para postear
         notificaciones; en versiones anteriores el sistema lo concede
         automáticamente al instalar la app. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- TODO(permissions): Add CAMERA permission if QR code scanning is needed
         <uses-permission android:name="android.permission.CAMERA" /> -->

    <!-- TODO(permissions): Add ACCESS_FINE_LOCATION for map/nearby hotel features
         <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
         <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->

    <application
        android:name=".TravelHubApp"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FCM service: recibe `onNewToken` (rotación del token) y los
             pushes entrantes via `onMessageReceived`. Si `google-services.json`
             no está presente en `app/`, FCM no se inicializa en runtime y este
             service simplemente no recibe callbacks — la app sigue corriendo
             sin push remoto, solo con notificaciones locales. -->
        <service
            android:name=".notifications.TravelHubFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="travelhub_general" />

        <!-- Required by AppCompatDelegate.setApplicationLocales to back-port
             per-app language switching to API < 33 and to coordinate with the
             system's LocaleManager on 33+. autoStoreLocales is "false" because
             we persist the chosen tag ourselves via UserPreferences (key
             "auth_locale") — letting AppCompat double-store would split the
             source of truth. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="false" />
        </service>
    </application>

</manifest>
